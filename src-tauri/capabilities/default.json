{
  "$schema": "https://schema.tauri.app/config/capability",
  "identifier": "default",
  "description": "Capability for default window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
